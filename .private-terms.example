# 私有詞彙清單範本 —— 複製成 .private-terms 後填入實際內容
#
# 這份檔案（.example）**進版控**，只放說明與通用範例；
# 實際清單 .private-terms **不進版控**，因為在公開 repo 裡放一份
# 「這些字不能出現」的清單，等於把答案寫在題目旁邊。
#
# 一行一個詞，# 開頭為註解。比對方式是「純字串包含」，不分大小寫以外的處理。
#
# 該放什麼：
#   · 不對外文件的檔名（含副檔名，例如 SOME_INTERNAL_DOC.md）
#   · 只有內部才會用的專有說法（專案代號、內部流程名稱）
#   · 一旦出現就代表寫太多的敘述性字眼
#
# 不該放什麼：
#   · 太通用的字（例如「文件」「說明」）——會讓每次提交都被擋，
#     最後大家習慣性 --no-verify，等於保護失效
#
# 範例（實際使用時請刪掉這幾行，換成真的內容）：
# INTERNAL_ONLY_DOC.md
# 某某內部代號

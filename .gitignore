# ══════════════════════════════════════════════════════════════════════════
# ⚠️ 注意：.gitignore 不支援「行尾註解」。
#    寫成 `data/processed/    # 說明` 會讓整行（含空白與 #）變成 pattern，
#    結果是這條規則完全不生效。註解一律獨立成行。
#    這個坑實際踩過：data/processed/ 因此被整包加進版控。
# ══════════════════════════════════════════════════════════════════════════

# ── 絕對不進版控 ──────────────────────────────────────────────────────────
# 其他專案的參考文件複本
既有資產對照/

# .env 含資料庫密碼與 API 金鑰。.env.example 才是團隊共享的規格檔。
.env
.env.local

# 未來若接入「真實客戶」文件，一律放這裡且不進版控。
# 目前 data/raw/CASE-* 全部是合成資料（假的），所以進版控 ——
# 同仁要能實際跑測試、要能挑出我們沒發現的錯誤，資料就必須跟得上程式碼。
data/raw/CLIENT-*/
data/confidential/

# ── 衍生產物：可由程式重新產生，不進版控（也避免 merge 衝突）──────────────
# data_update_finance.py 產生的清理後文字
data/processed/
# 增量更新的檔案指紋
data/.file_hashes/
# scripts/fetch_benchmarks.py 可重新下載（數百 MB）
data/benchmarks/
# SBA 原始 CSV 過大，由 fetch_real_corpus.py 重新下載
data/real/sba_7a_raw.csv
# 評測報告與技能檔產出
out/
llm_raw_outputs.log
_archive_v3/

# ── 環境 ─────────────────────────────────────────────────────────────────
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ipynb_checkpoints/

# ── 系統 ─────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# 瀏覽器「另存新檔」產生的資源目錄（css/js/圖片），不是知識內容
*_files/

# ── 執行記錄 ─────────────────────────────────────────────────────────────
# 評測與診斷的**結論**存成 .json 進版控（那是成果、要能被引用與複查），
# 逐行執行過程存成 .log 不進（那是雜訊，而且每次都不一樣）。
docs/*.log

# 編輯器工作區設定，與專案內容無關
.obsidian/
.vscode/
.idea/

# ── docs/ 與 out/ 的分工（別再讓兩邊出現同名檔案）────────────────────────
#
#   docs/   **交付文件**，進版控。人寫的或一次性產生後要被引用的：
#           README／SDD／評測結論 JSON 等。
#           這些檔案會被連結、被引用、被外部讀者看到。
#
#   out/    **程式每次執行的產出**，不進版控。同一支指令再跑一次就會覆蓋：
#           授信證據報告 PDF、暫時性的評測輸出。
#
# 判準：**再跑一次會不會變？** 會變就放 out/，不會變就放 docs/。
#
# 這條規則是被實際的混亂逼出來的：out/ 曾經有一份過期的 SKILL.md 複本，
# 與 skills/ 底下的正本同名但內容不同 —— 沒有人知道該看哪一份。
out/

# ── 不納入版控的本機文件 ────────────────────────────────────────────────
# 這些是團隊內部作業文件與尚在修訂中的產出，僅保留於本機。
# 排除清單本身不說明各檔用途——說明「藏了什麼」等於沒藏。
HANDOVER.md
ONE_PAGER.md
NEXT_SESSION_PROMPT.md
docs/APPLICATION_FORM_FIELDS.md
docs/BUSINESS_CASE.md
docs/MARKET_VALIDATION.md
docs/PROPOSAL.md
docs/PROPOSAL_SUBMISSION.md
docs/slides/
docs/images/proposal/
docs/FlowMind_*.docx
docs/FlowMind_*.pdf
FlowMind_AI_2026*.docx
scripts/patch_proposal_docx.py
scripts/build_submission_docx.py
scripts/build_submission_pdf.py
scripts/check_slide_overflow.py
*討論.txt

# 私有詞彙清單，供 scripts/check_public_safe.py 比對用（見該檔說明）
.private-terms
